# Frontend Environment Variables
# NEXT_PUBLIC_API_URL is used at build time if set
# If not set, the frontend will auto-detect the API URL at runtime
# by using the same hostname as the frontend with port 5000

# Optional: Set the API URL explicitly (e.g., for ngrok or custom domains)
# NEXT_PUBLIC_API_URL=http://localhost:5000
